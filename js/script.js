document.getElementById('donasi-form').addEventListener('submit', function(e) {
    e.preventDefault();
    // Logika untuk menambahkan proyek donasi ke dalam daftar
  });
  
  document.getElementById('barang-form').addEventListener('submit', function(e) {
    e.preventDefault();
    // Logika untuk menambahkan barang bekas ke dalam daftar
  });
  
  document.getElementById('forum-form').addEventListener('submit', function(e) {
    e.preventDefault();
    // Logika untuk menambahkan diskusi forum
  });
  